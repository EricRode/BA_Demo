package com.google.ar.core.examples.java.augmentedimage;

import android.media.Image;

import com.google.ar.core.Frame;
import com.google.ar.core.HitResult;
import com.google.ar.core.exceptions.NotYetAvailableException;

import java.util.List;

public class Ideas {


    //Frame frame = session.update();
    //Image image = frame.acquireCameraImage();

    // search image for rectangle

    // if rectangle detected search for text in image

    // get postion of text in image (coordinates)

    // hit test coordinates
    // List<HitResult> hitResultList = frame.hitTest(x, y);

    // if hit plane surface set Anchor


}
